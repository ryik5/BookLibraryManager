<UserControl x:Class="LibraryManager.Views.BookViewControls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    
    <UserControl.Resources>
        <Style TargetType="{x:Type Button}">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>
    </UserControl.Resources>

    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.4*"/>
            <ColumnDefinition Width="0.3*"/>
            <ColumnDefinition Width="0.3*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Style="{DynamicResource ButtonBorderStyle}" Margin="7 2 2 2">
            <Button Content="{Binding LoadingState}"
                Command="{Binding LoadBookContentCommand}" 
                IsEnabled="{Binding IsEnableLoad}"/>
        </Border>
        <Border Grid.Column="1" Style="{DynamicResource ButtonBorderStyle}" Margin="2 2 2 2">
            <Button Content="Save on disk"
                Command="{Binding SaveContentCommand}" 
                IsEnabled="{Binding IsEnableSave}"/>
        </Border>
        <Border Grid.Column="2" Style="{DynamicResource ButtonBorderStyle}" Margin="2 2 7 2">
            <Button Content="ClearContent"
                Command="{Binding ClearBookContentCommand}" 
                IsEnabled="{Binding IsEnableSave}"/>
        </Border>
    </Grid>

</UserControl>
