<UserControl x:Class="LibraryManager.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:view="clr-namespace:LibraryManager.Views"
             mc:Ignorable="d" 
             x:Name="mainView">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="34"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <view:LibraryControls Grid.Row="0"/>

        <Grid x:Name="dump" Width="1" Height="1" 
              Visibility="{Binding DataContext.LibraryVisibility, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"/>
        <view:LibraryView Grid.Row="1" 
                          DataContext="{Binding Library, UpdateSourceTrigger=PropertyChanged, NotifyOnSourceUpdated=True}" 
                          Visibility="{Binding ElementName=dump, Path=Visibility}"
                          VerticalAlignment="Top" />
    </Grid>

</UserControl>
