<Window x:Class="LibraryManager.Views.MessageBoxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:view="clr-namespace:LibraryManager.Views"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource DialogWindowStyle}"
        Width="500"
        Height="400"
        Background="{StaticResource ColorSet6}">

    <Grid Background="{StaticResource ColorSet6}"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        
        <ScrollViewer Grid.Row="0" VerticalContentAlignment="Stretch"
                      VerticalScrollBarVisibility="Auto" 
                      HorizontalScrollBarVisibility="Auto">
            <TextBlock Text="{Binding MessageBlock}"
                   FontSize="24"
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"
                   MinWidth="100"
                   MinHeight="100"
                   TextTrimming="CharacterEllipsis"/>
        </ScrollViewer>

        <view:ThreeButtonsPanel  Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="5 5 5 10"/>

    </Grid>
</Window>
