<Window x:Class="BookLibraryManager.TestApp.View.AddBookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookLibraryManager.TestApp.View"
        mc:Ignorable="d"
        xmlns:view="clr-namespace:BookLibraryManager.TestApp.View"
        Title="Add Book window" Height="300" Width="450">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5 5 5 5"/>
        </Style>
    </Window.Resources>

    <Grid HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <view:BookView DataContext="{Binding Book}"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Left" Margin="100 5 0 5">
            <Button Content="Add" Command="{Binding ExecuteCommand}" Width="85" Height="30"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Width="85" Height="30"/>
        </StackPanel>
        
    </Grid>
</Window>
