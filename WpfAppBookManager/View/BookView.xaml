<UserControl x:Class="BookLibraryManager.TestApp.View.BookView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:util="clr-namespace:BookLibraryManager.DemoApp.Util"
             xmlns:view="clr-namespace:BookLibraryManager.DemoApp.View"
             mc:Ignorable="d" >
    
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="MinWidth" Value="220"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <ControlTemplate x:Key="ValidationTemplate">
            <DockPanel>
                <TextBlock Text="!" Foreground="Red" FontSize="20" Padding="0 0 3 0"/>
                <AdornedElementPlaceholder/>
            </DockPanel>
        </ControlTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Author"/>
        <Border Grid.Row="0" Grid.Column="1" Style="{DynamicResource TextBoxBorderStyle}">
            <TextBox Text="{Binding Book.Author}" />
        </Border>

        <Label Grid.Row="1" Grid.Column="0" Content="Title"/>
        <Border Grid.Row="1" Grid.Column="1" Style="{DynamicResource TextBoxBorderStyle}">
            <TextBox Text="{Binding Book.Title}" />
        </Border>

        <Label Grid.Row="2" Grid.Column="0" Content="Pages"/>
        <Border Grid.Row="2" Grid.Column="1" Style="{DynamicResource TextBoxBorderStyle}">
            <TextBox Validation.ErrorTemplate="{StaticResource ValidationTemplate}">
                <TextBox.Text>
                    <Binding Path="Book.TotalPages" UpdateSourceTrigger="PropertyChanged" >
                        <Binding.ValidationRules>
                            <util:IntValidationRule Min="1" Max="1000000000"/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>

        </Border>

        <Label Grid.Row="3" Grid.Column="0" Content="Publish Date"/>
        <Border Grid.Row="3" Grid.Column="1" Style="{DynamicResource TextBoxBorderStyle}">
            <TextBox Validation.ErrorTemplate="{StaticResource ValidationTemplate}">
                <TextBox.Text>
                    <Binding Path="Book.PublishDate" UpdateSourceTrigger="PropertyChanged" >
                        <Binding.ValidationRules>
                            <util:IntValidationRule Min="400" Max="2100"/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
        </Border>

        <Label Grid.Row="4" Grid.Column="0" Content="Description"/>
        <Border Grid.Row="4" Grid.Column="1" Style="{DynamicResource TextBoxBorderStyle}">
            <TextBox Text="{Binding Book.Description}"/>
        </Border>

        <Label Grid.Row="5" Grid.Column="0" Content="Content"/>
        <view:BookViewControls Grid.Row="5" Grid.Column="1"/>

        <Label Grid.Row="6" Grid.Column="0" Content="Genre"/>
        <Border Grid.Row="6" Grid.Column="1" Style="{DynamicResource TextBoxBorderStyle}">
            <TextBox Text="{Binding Book.Genre}"/>
        </Border>

        <Label Grid.Row="7" Grid.Column="0" Content="ISDN"/>
        <Border Grid.Row="7" Grid.Column="1" Style="{DynamicResource TextBoxBorderStyle}">
            <TextBox Text="{Binding Book.ISDN}"/>
        </Border>

    </Grid>
</UserControl>
