<UserControl x:Class="BookLibraryManager.TestApp.View.ExecuteCancelPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5 5 5 5"/>
        </Style>
    </UserControl.Resources>

    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 5 0 5">
        <Button Content="{Binding ExecuteButtonName}" Command="{Binding ExecuteCommand}" Width="85" Height="30"/>
        <Button Content="Cancel" Command="{Binding CancelCommand}" Width="85" Height="30"/>
    </StackPanel>

</UserControl>
