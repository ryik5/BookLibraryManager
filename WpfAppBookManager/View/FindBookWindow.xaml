<Window x:Class="BookLibraryManager.TestApp.View.FindBookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:view="clr-namespace:BookLibraryManager.TestApp.View"
        mc:Ignorable="d"
        Title="FindBookWindow" Height="450" Width="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="0"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <view:FindBooksControl Grid.Row="0"/>

        <view:LibraryView Grid.Row="1" VerticalAlignment="Top" VerticalContentAlignment="Top" HorizontalContentAlignment="Stretch" HorizontalAlignment="Stretch"
                          DataContext="{Binding ., NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}"
                         />

        <!--<view:LogView Grid.Row="2" Margin="5" Padding="5 0 0 5"/>-->

        <view:StatusBarControl Grid.Row="3" VerticalAlignment="Top"
                               DataContext="{Binding StatusBarItems, NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}"
                                />

    </Grid>
</Window>
