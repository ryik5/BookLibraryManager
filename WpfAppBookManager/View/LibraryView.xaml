<UserControl x:Class="BookLibraryManager.TestApp.View.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookLibraryManager.TestApp.View"
             mc:Ignorable="d"
             Background="#DDDDDD">

    <Border Background="Transparent" BorderBrush="DarkGray" Margin="5 0 5 0" Padding="2" BorderThickness="1" CornerRadius="5">
        <ScrollViewer HorizontalAlignment="Stretch" VerticalAlignment="Top" VerticalScrollBarVisibility="Auto">
            <DataGrid Background="Transparent" BorderThickness="0"
                    ItemsSource="{Binding BookList, NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}"
                    SelectedItem="{Binding SelectedBook}"
                    DataGrid.IsReadOnly="True"
                    MinHeight="2"
                    >
            </DataGrid>
        </ScrollViewer>
    </Border>
</UserControl>
