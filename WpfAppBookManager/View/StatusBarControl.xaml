<UserControl x:Class="BookLibraryManager.TestApp.View.StatusBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Margin" Value="5 0 5 0"/>
        </Style>
    </UserControl.Resources>
    
    <Border Style="{DynamicResource ControlBorderStyle}">
        <StatusBar  Background="Transparent" BorderThickness="0">
            <StatusBarItem HorizontalAlignment="Left"  BorderThickness="0 0 1 0" BorderBrush="DarkGray">
                <TextBlock Text="{Binding StartPointText}"/>
            </StatusBarItem>
            <StatusBarItem >
                <TextBlock Text="{Binding TextInfoText}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right" BorderThickness="1 0 0 0" BorderBrush="DarkGray"
                           ToolTip="Total number of books in the current library">
                <TextBlock >
                    <Run Text="Book(s): " FontWeight="Bold"/>
                    <Run Text="{Binding TotalPagesText}"/>
                </TextBlock>
            </StatusBarItem>
        </StatusBar>
    </Border>
</UserControl>
